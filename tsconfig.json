{
  "include": [ //js로 compile하고 싶은 모든 디렉터리를 넣는다.
    "src" //ts가 src의 모든 파일을 확인한다는 것을 의미
  ],
  "compilerOptions": {
    "outDir": "build" // js파일이 생성될 디렉터리, Typescript에게 js 파일을 어디에 만들지 알려준다
  } // build 폴더에 만들어진 코드를 넣는다.
}